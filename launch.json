{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/${input:programPath}",
      "runtimeExecutable": "/Users/kamil/.nvm/versions/node/v22.12.0/bin/node",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ],
  "inputs": [
    {
      "type": "pickString",
      "id": "programPath",
      "description": "Select the program to launch",
      "options": [
        "app.js",
        "strategies/leaky_bucket.js"
      ]
    }
  ]
}